\* Tested on Ubuntu 24.04 (WSL2).

## Setup

- Open the Ubuntu app and complete the installation.

- ```shell
  sudo visudo
  ```

- ```shell
  sh <(curl -sL guyc.at/ubuntu.sh)
  ```

- ```shell
  wsl.exe --shutdown
  ```

- Open Windows Terminal.
